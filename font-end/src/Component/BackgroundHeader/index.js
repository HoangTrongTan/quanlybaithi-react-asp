import style from "./BackgroundHeader.module.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(style);

function BackgroundHeader() {
  return <div className={cx("s")}>

  </div>;
}

export default BackgroundHeader;
